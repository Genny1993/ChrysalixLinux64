	var: $code;
	
	print: 'Введите код на языке Chrysalix: ';
	input: $code;

	while: (change: $, true), (
		if: (iscode: $, $code), (
			pe: $, $code;
			break:;
		), 
		(
			print: 'Введенная строка не является корректным кодом, введите еще раз: ';
			input: $code;
		);
	);
	end: 0;